@model List<Web_QLKhachSan.Models.PhongAnh>
@{
    ViewBag.Title = "Index";
}
<link rel="stylesheet" href="~/assets/css/thuvien-anh.css" />

<!-- Hero Section -->
<section class="gallery-hero">
    <div class="hero-overlay"></div>
    <div class="hero-content">
        <h1 data-aos="fade-up">Thư Viện Ảnh</h1>
        <p data-aos="fade-up" data-aos-delay="100">
            Khám phá không gian sang trọng và đẳng cấp của The Serene Horizon Hotel
        </p>
    </div>
</section>

<!-- Gallery Section -->
<section class="gallery-section">
    <div class="container">
        <div class="gallery-grid">
            @if (Model != null && Model.Any())
            {
                int index = 0;
                foreach (var phongAnh in Model)
                {
                    int delay = (index % 8) * 100;
                    <div class="gallery-item" data-index="@index" data-aos="zoom-in" data-aos-delay="@delay">
                        <div class="gallery-card">
                            <img src="@phongAnh.Url" alt="@(phongAnh.Phong != null ? phongAnh.Phong.TenPhong : "Hình ảnh khách sạn")" />
                            <div class="gallery-overlay">
                                <div class="gallery-info">
                                    <span class="gallery-category">
                                        <i class="fas fa-bed"></i> @(phongAnh.Phong != null ? phongAnh.Phong.TenPhong : "The Serene Horizon Hotel")
                                    </span>
                                </div>
                                <button class="view-btn" onclick="openLightbox(@index)">
                                    <i class="fas fa-search-plus"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                    index++;
                }
            }
            else
            {
                <div class="no-images-message">
                    <p>Chưa có hình ảnh nào trong thư viện.</p>
                </div>
            }
        </div>
    </div>
</section>

<!-- Lightbox Modal -->
<div class="lightbox" id="lightbox">
    
    <button class="lightbox-prev" onclick="changeSlide(-1)">
        <i class="fas fa-chevron-left"></i>
    </button>
    <button class="lightbox-next" onclick="changeSlide(1)">
        <i class="fas fa-chevron-right"></i>
    </button>
    <div class="lightbox-content">
        <img id="lightbox-img" src="" alt="" />
        
    </div>
    <div class="lightbox-counter">
        <span id="lightbox-current">1</span> / <span id="lightbox-total">@(Model?.Count ?? 0)</span>
    </div>
</div>



<!-- CTA Section -->
<section class="cta-section">
    <div class="container">
        <div class="cta-content" data-aos="fade-up">
            <h2>Đặt Phòng Ngay Hôm Nay</h2>
            <p>
                Trải nghiệm đẳng cấp 5 sao với không gian sang trọng, dịch vụ chuyên nghiệp
                và những tiện ích hiện đại. Chúng tôi cam kết mang đến cho bạn kỳ nghỉ đáng nhớ nhất.
            </p>
            <a href="@Url.Action("Index", "DatPhong")" class="cta-button">
                <i class="fas fa-calendar-plus"></i>
                <span>Đặt Phòng Ngay</span>
            </a>
        </div>
    </div>
</section>

<script src="~/assets/js/thuvien-anh.js"></script>

